    // escreva uma função que vai contando de 1 até o número que vc enviou
    
    const contando = (number) => {
        for (i = 1; i <= number; i++) {
            console.log(`Contando... ${i}`)
        }
    }

    contando(10)