// A melhor forma de armazenar um elemento ou um valor é em uma variável

const h1 = document.getElementById('titulo1');

// "getElementById" chama um elemento específico, um id de algum elemento de uma página HTML, por isso que i ID de algum elemento deve ser único

// usamos no nome "document" para especificar ao JavaScript que o que a gente quer esta em uma pasta HTML

console.log(h1);

const h2 = document.getElementById('titulo2');

console.log(h2);