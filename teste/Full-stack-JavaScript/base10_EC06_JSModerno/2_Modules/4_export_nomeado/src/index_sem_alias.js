// Importamos dessa forma:
import { mult, soma } from "./export_nomeado";
// Diferente do export default, esse precisamos usar o nome expecífico

console.log(mult(2, 5)) // 10

console.log(soma(2, 5)) // 7